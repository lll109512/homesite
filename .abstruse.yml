image: node-image

matrix:
  - env: c=1

install:
  -  sudo apt-get install python3-venv -y
  -  sudo apt install python3-pip
  -  python3 -m venv venv
  -  . venv/bin/activate
  -  pip3 install -r requirements.txt

script:
  - . venv/bin/activate
  -  pytest

cache:
  - python_model