image: node-image

matrix:
  - env: c=1

install:
  -  python3 -m venv venv
  -  . venv/bin/activate
  -  pip install -r requirements.txt

script:
  - . venv/bin/activate
  -  pytest

cache:
  - python_model